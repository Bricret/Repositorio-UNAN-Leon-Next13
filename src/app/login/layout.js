export const metadata = {
    title: 'Login | Repositorio',
    description: 'Inicio de sesion del repositorio',
  }

export default function Layout({ children }) {

    return (
        <>
            {children}
        </>

    );
}