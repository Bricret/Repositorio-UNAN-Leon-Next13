export const metadata = {
    title: 'Buscador | Repositorio',
    description: 'Paginal principal del repositorio',
  }

export default function Layout({ children }) {

    return (
        <>
            {children}
        </>

    );
}